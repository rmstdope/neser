; Clean-room SNES APU IPL source (64 bytes).
; The committed ipl.bin is embedded by Rust at build time.
;
; Byte sequence corresponds to the documented bootstrap handshake routine.
; Source is kept for traceability and future assembler-based regeneration.

; Not assembled in CI yet.
