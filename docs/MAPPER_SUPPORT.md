# Mapper Support

This document catalogues every mapper implemented in **neser** and cross-references each one
against the [NesDev wiki](https://www.nesdev.org/wiki/) specification. Where the implementation
deviates from the specification the delta is noted. A separate section lists commonly-needed
mappers that are not yet implemented.

> **Legend**
>
> | Symbol | Meaning |
> |--------|---------|
> | ✅ | Fully implemented, no known delta |
> | ⚠️ | Implemented with known limitations / partial deltas |
> | ❌ | Not implemented (stub or missing) |

---

## Implemented Mappers

| # | Hardware Name | PRG Banking | CHR Banking | Mirroring | IRQ | Audio | PRG-RAM | Status | Delta / Notes |
|---|---------------|-------------|-------------|-----------|-----|-------|---------|--------|---------------|
| 0 | NROM | Fixed 16 or 32 KB | Fixed 8 KB ROM | Header | None | None | Optional 2–8 KB | ✅ | Family BASIC PRG-RAM present in neser. No deltas. |
| 1 | MMC1 (SxROM) | 16 KB or 32 KB switchable | 4 KB or 8 KB switchable | H / V / 1scA / 1scB | None | None | Present, disable-able | ⚠️ | **Submappers 5 (SEROM/SHROM – fixed 32 KB PRG), 6 (2ME EEPROM Famicom Network System) and 7 (Kaiser KS-7058 hard-wired NT) are not handled.** MMC1A vs MMC1B PRG-RAM disable bit difference unverified. SUROM/SXROM 512 KB PRG-ROM (CHR bank bits extend PRG address) not tested. |
| 2 | UxROM | 16 KB switchable + last fixed | 8 KB CHR-RAM | Header | None | None | None | ✅ | NES 2.0 submappers (bus-conflict disambiguation) informational only; bus conflicts not emulated – acceptable for gameplay. |
| 3 | CNROM | Fixed 32 KB | 8 KB switchable ROM | Header | None | None | None | ⚠️ | Submapper bus-conflict modes (sub 1 = no conflicts, sub 2 = AND conflicts) not distinguished. Mapper 185 CHR-disable anti-piracy extension unimplemented. Hayauchi Super Igo PRG-RAM not handled (edge case). |
| 4 | MMC3 / TxROM | 8 KB × 2 switchable + 2 fixed | 2 KB × 2 + 1 KB × 4 | H / V | Scanline (A12) | None | Optional, write-protect | ⚠️ | **IRQ Sharp vs NEC mode selected via CRC heuristic, not NES 2.0 submapper.** MMC6 (submapper 1) uses a different PRG-RAM protect scheme – not properly distinguished. Waixing T9552 (submapper 5, scrambled CHR) unimplemented. |
| 5 | MMC5 (ExROM) | 8 / 16 / 32 KB flexible | 1 / 2 / 4 / 8 KB flexible | H / V / 1sc / 4-screen | CPU-cycle | 8-ch PCM | Up to 64 KB | ⚠️ | **Sunsoft 5B expansion audio (YM2149) present on 5B variant – irrelevant to MMC5.** Vertical split ($5200–$5202) registers present but split-screen rendering not fully functional. Extended attribute mode ($5104 = 01) partially implemented. $5130 upper CHR bank bits need verification. MMC5A extra regs ($5207–$520A) unimplemented (no commercial games use them). |
| 6 | Super Magic Card | Complex bank modes via $4500-style regs | 1 / 2 / 4 / 8 KB | H / V switchable | None | None | Banked WRAM | ⚠️ | **Trainer JSR $7003 execution not implemented.** Submapper 0 = loader at $7000. Unusual hardware; no other emulator fully emulates all modes. |
| 7 | AxROM | 32 KB switchable | 8 KB CHR-RAM | 1-screen switchable | None | None | None | ✅ | NES 2.0 submappers (AMROM/AOROM with bus conflicts vs ANROM without) not distinguished – bus conflicts not emulated; acceptable. |
| 8 | Super Magic Card (variant) | As mapper 6 | As mapper 6 | As mapper 6 | None | None | WRAM | ⚠️ | Shares implementation with mapper 6; trainer not executed. |
| 9 | MMC2 (PxROM) | 8 KB switchable + 3 fixed | 4 KB latch-switched × 2 | H / V | None | None | None | ✅ | Latch 0 fires only at $0FD8 (single address); latch 1 fires at $1FD8–$1FDF (range). Confirmed correct. |
| 10 | MMC4 (FxROM) | 16 KB switchable + last fixed | 4 KB latch-switched × 2 (symmetric) | H / V | None | None | None | ✅ | Symmetric latch behavior for both pattern tables. PRG-RAM at $6000 fixed. No deltas. |
| 11 | Color Dreams | 32 KB switchable | 8 KB switchable | Header | None | None | None | ✅ | Bus conflicts present on real hardware but not emulated – acceptable. No deltas. |
| 12 | SL-5020B (MMC3 + CHR A18 extension) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper12.rs`; constructor routed via `Mapper12::new`. |
| 13 | CPROM (Videomation) | Fixed 32 KB | Lower 4 KB fixed to bank 0, upper 4 KB via 2-bit selector | Fixed vertical | None | None | None | ✅ | Bus conflicts not emulated – acceptable. No deltas. |
| 14 | SL1632 (MMC3/VRC2 hybrid) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper14.rs`; constructor routed via `Mapper14::new`. |
| 15 | 100-in-1 Multicart | 4 modes: NROM-256 / UNROM / NROM-64 / NROM-128 | 8 KB CHR-RAM | H / V switchable | None | None | None | ⚠️ | **CHR-RAM write-protect (applied in PRG modes 0 and 3) may not be enforced.** PRG-RAM not present on real hardware – emulator addition for hacks. |
| 16 | Bandai FCG (LZ93D50 / FCG-1/2) | 16 KB switchable + last fixed | 8 × 1 KB switchable | H / V / 1scA / 1scB | CPU-cycle (IRQ latch/counter) | None | None (sub 3 = 8 KB WRAM; use mapper 153) | ⚠️ | **24C02 EEPROM save (Dragon Ball Z II/III/Gaiden, SD Gundam stories, etc.) not implemented – saves broken for those games.** Submappers 1 (24C01 → use mapper 159), 2 (Datach → use mapper 157), 3 (WRAM → use mapper 153) deprecated per NES 2.0 and not routed. FCG-1/2 (sub 4) vs LZ93D50 (sub 5) IRQ latch behavior differs: sub 4 writes directly to counter, sub 5 uses a latch copied on $800A write – verify neser handles both paths. |
| 17 | Super Magic Card (variant) | As mapper 6 | As mapper 6 | As mapper 6 | None | None | WRAM | ⚠️ | Shares implementation with mapper 6; trainer not executed. |
| 18 | Jaleco SS 88006 | 8 KB × 3 switchable + last 8 KB fixed | 8 × 1 KB switchable | H / V / 1scA / 1scB switchable | CPU-cycle down-counter (4/8/12/16-bit) | None | None | ✅ | Nibble-paired PRG/CHR bank registers (`addr & 0xF003` decode), IRQ reload/ack via `$F000/$F001`, and 4-way mirroring at `$F002` are implemented. |
| 19 | Namco 163 (N163) | 8 KB × 3 switchable + last fixed | 8 × 1 KB switchable; banks $E0–$FF map CIRAM | H / V / CIRAM-switched | 15-bit CPU-cycle up-counter | 8-channel wavetable | 128 B chip RAM (battery-backed) | ⚠️ | **$F800 PRG-RAM write-protect register (key $40–$4E) may not be implemented.** NT-via-CHR-bank (register values $E0–$FF enabled by $E800 bits 6/7) needs verification. Audio submapper volume levels (0–5) are informational only. Pin 22 open-collector behaviour unverified. |
| 20 | Famicom Disk System (FDS) | 32 KB work RAM ($6000–$DFFF) | 8 KB CHR-RAM | Controlled by $4025 bit 3 (default Horizontal) | 16-bit CPU-cycle down-counter ($4020–$4022) | Wave-table ($4040–$408F, backed but not mixed) | 32 KB work RAM | ⚠️ | **Disk I/O not emulated: .fds disk image loading, disk reads/writes and CRC are not implemented.** BIOS ROM must be supplied as PRG-ROM; no user-supplied BIOS path. FDS audio (wave table + modulation channel) registers are stored but not mixed into the audio output. |
| 21 | Konami VRC4a/VRC4c | 8 KB × 2 switchable + 2 fixed | 8 × 1 KB switchable | H / V / 1sc switchable | CPU-cycle / scanline latch-based | None | 8 KB optional | ⚠️ | VRC2 microwire EEPROM latch read-back at $6000 (required for Contra/Goemon 2 on some boards) unverified. WRAM enable bit at $9002 (VRC4 only) – verify neser honours it. |
| 22 | Konami VRC2a | 8 KB × 2 switchable + 2 fixed | 8 × 1 KB switchable (CHR shifted right by 1) | H / V switchable | None | None | None | ⚠️ | CHR right-shift-by-1 (A0 ignored on CHR registers) must be applied. Bit 0 of register address ignored on all VRC2a writes. |
| 23 | Konami VRC2b/VRC4e/VRC4f | 8 KB × 2 switchable + 2 fixed | 8 × 1 KB switchable | H / V / 1sc switchable | CPU-cycle / scanline | None | 8 KB optional | ⚠️ | Address bit mapping disambiguated by NES 2.0 submappers. WRAM control at $9002 (VRC4 variants). Same delta as mapper 21. |
| 24 | Konami VRC6a | 8 KB × 3 switchable + last fixed | 8 × 1 KB or 16 × 0.5 KB switchable | H / V / 1sc / ROM-NT switchable | CPU-cycle or scanline (M bit) | 3-ch pulse/sawtooth | None | ✅ | $B003 PPU banking style register (H/V/1scA/1scB) confirmed. Commercial games use only mirroring modes 0–3 which are implemented. VRC6a vs VRC6b (A0/A1 swapped for mapper 26) handled by separate mapper entry. Audio implemented. |
| 25 | Konami VRC4b/VRC4d | 8 KB × 2 switchable + 2 fixed | 8 × 1 KB switchable | H / V / 1sc switchable | CPU-cycle / scanline | None | 8 KB optional | ⚠️ | Same A0/A1 swapping considerations as VRC2/VRC4 family. WRAM enable bit at $9002. Same delta as mapper 21/23. |
| 26 | Konami VRC6b | 8 KB × 3 switchable + last fixed | 8 × 1 KB or 16 × 0.5 KB | H / V / 1sc / ROM-NT | CPU-cycle or scanline | 3-ch pulse/sawtooth | None | ✅ | A0/A1 swapped relative to VRC6a (mapper 24). Implemented as separate mapper entry. Audio implemented. |
| 27 | Vrc2Vrc4Mapper | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Scanline | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/vrc2_vrc4.rs`; constructor routed via `Vrc2Vrc4Mapper::new`. |
| 28 | Action 53 (homebrew multicart) | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper28.rs`; constructor routed via `Mapper28::new`. |
| 29 | Sealie Computing | 16 KB switchable at `$8000` + last 16 KB fixed | 8 KB CHR-RAM switchable (4 banks) | Fixed from header | None | None | 8 KB WRAM | ✅ | Register writes at `$8000-$FFFF` use bits `[..PPP PCC]` for PRG/CHR selection. Uses 32 KB CHR-RAM and fixed mirroring from header. |
| 30 | UNROM-512 (homebrew) | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper30.rs`; constructor routed via `Mapper30::new`. |
| 31 | NSF playing cart / 8-in-1 (Homebrew) | 4 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper31.rs`; constructor routed via `Mapper31::new`. |
| 32 | Irem G-101 | 8 KB × 2 switchable + 2 fixed; mode 1 swaps $8000/$C000 | 8 × 1 KB switchable | H / V via bit; hardwired 1-screen on Major League | None | None | None | ⚠️ | **NES 2.0 submapper for Major League (hardwired 1-screen + PRG mode 1 disabled) must be handled via hash or submapper header.** PRG mode 1 ("愛先生" game) rare but present. |
| 33 | Taito TC0190 | 8 KB × 2 switchable + 2 fixed | 2 × 2 KB + 4 × 1 KB switchable | H / V via bit in $8000 | None | None | None | ✅ | Unlike mapper 48 (TC0350), no IRQ. Note: many mapper 48 dumps were mislabelled as 033 – ensure correct routing. No deltas. |
| 34 | BNROM / NINA-001 | BNROM: 32 KB switchable; NINA: 32 KB switchable | BNROM: 8 KB CHR-RAM; NINA: 4 KB × 2 switchable | Fixed (solder pads) | None | None | BNROM: none; NINA: 8 KB PRG-RAM | ⚠️ | NES 2.0 submapper 1 = strict NINA-001 (bit0 PRG at $7FFD, CHR at $7FFE/$7FFF), submapper 2 = BNROM. **Submapper 0 / iNES fallback uses CHR-ROM presence (CHR present → NINA path, no CHR → BNROM).** For submapper 0 NINA hacks, FCEUX-compatible hybrid behavior is enabled ($8000-$FFFF PRG writes accepted, $7FFD uses full register value). BNROM AND-type bus conflicts are emulated. NINA registers at $7FFD/$7FFE/$7FFF overlap PRG-RAM. |
| 35 | J.Y. Company (simple variant) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper35.rs`; constructor routed via `Mapper35::new`. |
| 36 | TXC 01-22000-400 (PCM063 family) | 32 KB switchable via TXC output (`RR[1:0]`) | 8 KB switchable via `$4200` register | Fixed from header | None | None | None | ✅ | Implements masked TXC register window at `$4100-$5FFF` plus PRG apply strobe at `$8000`. Includes `$4100` status read bits `[5:4]` and save-state register roundtrip. |
| 37 | ZZ board MMC3 multicart (Super Mario Bros + Tetris + Nintendo World Cup) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | None | ⚠️ | Implemented in `src/cartridge/mapper37.rs`; constructor routed via `Mapper37::new`. |
| 38 | Crime Busters / 32-in-1 (discrete logic) | 32 KB switchable (`$7000–$7FFF` bits [1:0]) | 8 KB switchable (`$7000–$7FFF` bits [3:2]) | Fixed from header | None | None | None | ✅ | Single write-only register at `$7000–$7FFF`. No PRG-RAM. No known deltas. |
| 39 | BMC-STUDYNGAME (Study and Game 32-in-1) | 32 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper39.rs`; constructor routed via `Mapper39::new`. |
| 40 | NTDEC 2722 (SMB2J FDS conversion) | Fixed banks at $6000/$8000/$A000/$E000 + switchable $C000 | 8 KB CHR-RAM | Fixed via header | CPU-cycle (4096 cycles fixed) | None | None | ⚠️ | **Submapper 1 (NTDEC 2752 multicart) outer bank register at $C000-$DFFF not implemented.** IRQ fires 4096 cycles after enable (CD4020 13-bit counter, self-acknowledges after another 4096 cycles). |
| 41 | Caltron 6-in-1 | 32 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper41.rs`; constructor routed via `Mapper41::new`. |
| 42 | FDS cartridge conversion | 8 KB switchable at $6000, last 32 KB fixed | 8 KB switchable ROM or CHR-RAM | H / V switchable | CPU-cycle (fires after 24576 cycles) | None | None | ✅ | IRQ is a 15-bit counter; asserted while two MSBs are set (24576 on, 8192 off cycle). CHR-RAM used by Bio Miracle Bokutte Upa. No deltas. |
| 43 | TONY-I / YS-612 (Super Mario Bros. 2 FDS conversion) | 8 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper43.rs`; constructor routed via `Mapper43::new`. |
| 44 | Super Big 7-in-1 (MMC3 multicart) | MMC3 8 KB PRG banks within 128/256 KB blocks | MMC3 CHR within 128/256 KB blocks | MMC3 H / V | MMC3 scanline | None | None | ✅ | Block select in $A001 bits [2:0]. Block 6/7 = 256 KB. 7 selects same as 6. No PRG-RAM. No deltas. |
| 45 | GA23C multicart | MMC3 8 KB PRG + outer PRG-AND/PRG-OR | MMC3 CHR + outer CHR-AND/CHR-OR | MMC3 H / V | MMC3 scanline | None | Via MMC3 WRAM bits | ✅ | Four sequential outer bank registers at $6000. Register lock bit prevents further writes. DIP-switch menu selection ($5000–$5FFF read) for 超强年度新卡 multicart present. No deltas. |
| 46 | Rumble Station (Color Dreams multicart) | 32 KB page via outer [$6000 bits 3:0] + inner [$8000 bit 0] | 8 KB bank via outer [$6000 bits 7:4] + inner [$8000 bits 6:4] | Fixed by header | None | None | None | ✅ | $6000–$7FFF is outer register (no PRG-RAM). At power-up outer = 0. No PRG-RAM. No deltas. |
| 47 | Super Spike V'Ball + World Cup (MMC3 2-game) | MMC3 within 128 KB blocks (1-bit block select) | MMC3 CHR within 128 KB blocks | MMC3 H / V | MMC3 scanline | None | None | ✅ | Block register at $6000–$7FFF writable only when MMC3 PRG-RAM is enabled+writable. Each block = 128 KB PRG + 128 KB CHR. No deltas. |
| 48 | Mapper48 | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper48.rs`; constructor routed via `Mapper48::new`. |
| 49 | Super HIK 4-in-1 (MMC3 multicart) | MMC3 within 128 KB blocks; 32 KB mode when O=0 | MMC3 CHR within 128 KB blocks | MMC3 H / V | MMC3 scanline | None | None | ✅ | Multicart register at $6000–$7FFF. Mode bit O: 0 = entire 32 KB fixed (ignores MMC3 PRG regs), 1 = MMC3 PRG normal. Block = BB bits. No deltas. |
| 50 | SMB2J N-32 (FDS conversion) | PRG scrambled [HLLM] at $4020; fixed $6000/$8000/$A000/$E000; switchable $C000 | 8 KB CHR-ROM/RAM | Fixed by header | CPU-cycle (fires at 4096 cycles, $0FFF→$1000) | None | None | ✅ | Register mask $4120. IRQ enable at $4120[0]; disable also acknowledges and resets counter. PRG register unscrambling implemented. No deltas. |
| 51 | 11-in-1 Ball Games | 8 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Dynamic / mapper-controlled | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper51.rs`; constructor routed via `Mapper51::new`. |
| 52 | BMC Realtec 8213 MMC3 multicart | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper52.rs`; constructor routed via `Mapper52::new`. |
| 53 | Supervision 16-in-1 | 16 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper53.rs`; constructor routed via `Mapper53::new`. |
| 54 | Novel Diamond | 32 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper54.rs`; constructor routed via `Mapper54::new`. |
| 55 | BTL-MARIOBABY (Mario Baby bootleg) | 8 KB switchable at $6000, last 32 KB fixed | 8 KB fixed (no switching) | H / V switchable | CPU-cycle (fires after 24576 cycles) | None | None | ✅ | Registers at $F000–$FFFF only (addr & 3: 0=PRG latch, 1=mirror, 2=IRQ). IRQ: 15-bit counter, level-sensitive, fires while ≥ $6000. Disable also acknowledges and resets counter. No deltas. |
| 56 | Kaiser KS202 (Pirate SMB3) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper56.rs`; constructor routed via `Mapper56::new`. |
| 57 | BMC GK-192 multicart | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper57.rs`; constructor routed via `Mapper57::new`. |
| 58 | BMC multicart (address latch) | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper58.rs`; constructor routed via `Mapper58::new`. |
| 59 | BMC-T3H53 / BMC-D1038 | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper59.rs`; constructor routed via `Mapper59::new`. |
| 60 | Reset-based NROM-128 4-in-1 | 16 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper60.rs`; constructor routed via `Mapper60::new`. |
| 61 | NTDEC address latch multicart | 16 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Dynamic / mapper-controlled | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper61.rs`; constructor routed via `Mapper61::new`. |
| 62 | Super 700-in-1 (address latch + data latch) | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper62.rs`; constructor routed via `Mapper62::new`. |
| 63 | BMC multi-game bank switching | 16 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper63.rs`; constructor routed via `Mapper63::new`. |
| 64 | Tengen RAMBO-1 | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper64.rs`; constructor routed via `Mapper64::new`. |
| 65 | Irem H3001 | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper65.rs`; constructor routed via `Mapper65::new`. |
| 66 | GxROM (GNROM / MHROM) | 32 KB switchable | 8 KB switchable | Fixed H or V (solder pads / header) | None | None | None | ✅ | Bus conflicts not emulated – acceptable. MHROM uses only 2 PRG banks (bit 5 = 0). No deltas. |
| 67 | Mapper67 | 16 KB granularity (mapper-specific) | 2 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper67.rs`; constructor routed via `Mapper67::new`. |
| 68 | Sunsoft-4 | 16 KB switchable + last fixed | 4 × 2 KB switchable ROM; CHR ROM can replace nametables | H / V / 1scA / 1scB switchable | None | None | 8 KB PRG-RAM | ⚠️ | **ROM nametable mode ($E000 bit 4 = 1, nametable banks from $C000/$D000) must be implemented for After Burner and Maharaja.** Licensing IC timer at $6000 for Nantettatte!! Baseball not emulated (internal/external ROM switching) – that game is unlikely to be played. PRG-RAM enable bit in $F000 bit 4. |
| 69 | Sunsoft FME-7 / 5A / 5B | 8 KB × 4 switchable + last fixed; $6000 can be PRG-ROM or PRG-RAM | 8 × 1 KB switchable | H / V / 1scA / 1scB switchable | CPU-cycle 16-bit (separate counter-enable and IRQ-enable) | **5B: YM2149 (Sunsoft 5B audio)** | Up to 512 KB (FME-7) / 256 KB (5A/5B) switchable | ⚠️ | **Sunsoft 5B YM2149 expansion audio not implemented → Gimmick! (and Hebereke, Gremlins 2 JP) missing music tracks.** PRG bank 0 at $6000: RAM/ROM select and enable bits in register $8 – verify PRG-RAM enable handled. |
| 70 | Mapper70 | 16 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper70.rs`; constructor routed via `Mapper70::new`. |
| 71 | Camerica (BF9093/BF9096/BF9097) | 16 KB switchable + last fixed | 8 KB CHR-RAM | Hardwired H or V (most); 1-screen switchable (Fire Hawk only) | None | None | None | ⚠️ | **NES 2.0 submapper 1 (Fire Hawk, BF9097) uses mirroring register at $8000–$9FFF (bit 4). Most games do not have this register. Verify that neser applies the 1-screen mirroring only when writing to $8000–$9FFF (not $C000–$FFFF).** MiG 29 uses DMC IRQs and is picky about timing (APU concern, not mapper). |
| 72 | Jaleco JF-17 | 16 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper72.rs`; constructor routed via `Mapper72::new`. |
| 73 | Konami VRC3 | 16 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper73.rs`; constructor routed via `Mapper73::new`. |
| 74 | MMC3 variant with CHR-RAM at banks 8–9 (Chinese pirate boards) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper74.rs`; constructor routed via `Mapper74::new`. |
| 75 | Konami VRC1 | 8 KB granularity (mapper-specific) | 4 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper75.rs`; constructor routed via `Mapper75::new`. |
| 76 | Namco 109 (Megami Tensei: Digital Devil Story) | 8 KB × 2 switchable ($8000/$A000) + 2 fixed to last | 4 × 2 KB switchable (R2–R5); R0/R1 unused | Fixed from header | **None** | None | None | ✅ | Namco 108 variant with all-2 KB CHR banking. R2→$0000, R3→$0800, R4→$1000, R5→$1800. PRG/CHR mode bits forced to 0. All $8000–$FFFF writes redirect to $8000/$8001 only. No IRQ, no programmable mirroring. |
| 77 | IREM NINA-03 (Napoleon Senki) | Fixed 32 KB | 4 KB switchable CHR-ROM ($0000–$0FFF); 2 KB fixed CHR-RAM ($1000–$17FF) | One-screen lower (fixed) | None | None | None | ✅ | Register: any write to $8000–$FFFF; bits [3:0] select the 4 KB CHR-ROM page. Uses internal CIRAM nametables with fixed one-screen-lower mirroring; the 2 KB CHR-RAM is used only for pattern data. No known deltas from current implementation. |
| 78 | Holy Diver / Uchuusen – Cosmo Carrier | 16 KB switchable + last fixed | 8 KB switchable CHR-ROM | Mapper-controlled (bit 3) | None | None | None | ⚠️ | **Bit 3 mirroring interpretation differs by game: Holy Diver (NES 2.0 submapper 3) → 0 = H, 1 = V; Uchuusen/Cosmo Carrier (NES 2.0 submapper 1) → 0 = 1scA, 1 = 1scB. Without submapper info, the iNES1 "alternative nametables" flag is used as a heuristic, but this is unreliable. Submapper support required for correct behaviour.** Bus conflicts not emulated. |
| 79 | NINA-03/NINA-06 | 32 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper79.rs`; constructor routed via `Mapper79::new`. |
| 80 | Taito X1-005 | 8 KB granularity (mapper-specific) | 1 KB Switchable | Dynamic / mapper-controlled | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper80.rs`; constructor routed via `Mapper80::new`. |
| 81 | NTDEC N715021 (Super Gun) | 16 KB switchable + last fixed; 8 KB CHR switchable; mirroring fixed | None | None | None | ✅ | Address-latch mapper: bits [3:2] of write address select PRG bank; bits [1:0] select CHR bank. Data byte ignored. No deltas. |
| 82 | Taito X1-017 (Kyuukyoku Harikiri Stadium II/III) | 3 × 8 KB switchable PRG + last fixed; CHR 2×2 KB + 4×1 KB (mode-selectable); H/V switchable; 5 KB password-protected PRG-RAM | None | None | 5 KB (password-gated) | ✅ | Registers at $7EF0–$7EFF. RAM split into three independently password-protected regions. CHR mode bit swaps 2 KB and 1 KB halves. PRG bank = write-value >> 2. No deltas. |
| 83 | Caltron 6-in-1 (Dragon Ball Z Party multicart) | 4 × 8 KB switchable PRG (or 32 KB mode); 8 × 1 KB or 4 × 2 KB switchable CHR; H/V/1scA/1scB programmable; 16-bit CPU-cycle IRQ | None | None | None | ✅ | Registers at $5000/$5100–$5103/$8000–$8317. PRG 8-bank mode via $8300–$8302; 32 KB mode via $8000/$8100 bit 6. CHR 1 KB default; 2 KB mode set by $8000 write; $8312–$8315 clears 2 KB mode. IRQ counter written $8200/$8201; enabled by $8100 bit 7 latch. DIP switches at $5000 not emulated (always 0). |
| 84 | PC-SMB2J (Super Mario Bros. 2 Japan FDS conversion) | 3 fixed 8 KB banks (4/5/7) + 1 switchable (via $E000); fixed bank 6 at $6000 | 8 KB fixed | Fixed from header | CPU-cycle (4096-cycle, self-ack at 8192) | None | None | ✅ | NesDev spec says "details unknown; maybe same FDS port as Mapper 40". Implemented following Mapper 040 (NTDEC 2722) layout. $8000=IRQ disable/ack, $A000=IRQ enable, $E000=bank select. |
| 85 | Konami VRC7 | 3 × 8 KB switchable ($8000/$A000/$C000) + last 8 KB fixed at $E000 | 8 × 1 KB switchable ($0000–$1FFF) | H / V / 1scA / 1scB via $E000 bits[1:0] | CPU-cycle (VrcIrq, $E008/$F000/$F008) | YM2413/OPLL FM (stubbed) | 8 KB at $6000–$7FFF (enabled by $E000 bit 7) | ⚠️ | FM audio (YM2413/OPLL) is stubbed with silence. Address bit 4 remapped to bit 3 per VRC7 PCB wiring (except $9010). |
| 86 | Jaleco JF-13 (Moero!! Pro Yakyuu) | 32 KB switchable at $8000–$FFFF via bits[5:4] of register | 8 KB switchable via bits[1:0] ∥ (bit[6]→bit[2]); up to 64 KB | Fixed from header | None | 8 KB at $7000–$7FFF (not emulated) | None | ⚠️ | Register at $6000–$6FFF. Audio expansion at $7000–$7FFF is not emulated. |
| 87 | Jaleco/Konami CHR-only (City Connection, Choplifter, Hyper Olympic) | Fixed 32 KB at $8000–$FFFF | 8 KB switchable via swapped bits (bit 0→A13, bit 1→A14); up to 32 KB | Fixed from header | None | None | None | ✅ | Register at $6000–$7FFF. Bits[1:0] of write value select CHR bank with bit swap: bank = `((v&1)<<1)|((v&2)>>1)`. No PRG switching. |
| 88 | Namco 118 / Namco 108 (CHR A12→A16) (Dragon Spirit, Youkai Douchuuki) | 8 KB × 2 switchable ($8000/$A000) + 2 fixed to last | Lower 64 KB: 2 KB × 2 (R0/R1, 6-bit); Upper 64 KB: 1 KB × 4 (R2–R5, bit 6 forced) | Fixed from header | **None** | None | None | ✅ | Mapper 206 variant: CHR A12 wired to CHR A16 splits CHR-ROM into two halves. R0/R1 select 2 KB banks (6-bit mask) from lower 64 KB; R2–R5 select 1 KB banks with bit 6 always set from upper 64 KB. PRG/CHR mode bits forced to 0. All $8000–$FFFF writes redirect to $8000/$8001 only. No IRQ, no programmable mirroring. |
| 90 | J.Y. Company ASIC (Famicom Zhan Shi, Aladdin, Pocahontas) | 32 KB (mode 0), 16 KB × 2 (mode 1), 16 KB × 1 + fixed (mode 2), 8 KB × 4 (mode 3); modes via `$D000` bits 0–1 | 1 KB × 8 (mode 3), 2 KB × 4 (mode 2), 4 KB × 2 (mode 1), 8 KB × 1 (mode 0); modes via `$D000` bits 3–4; 9-bit banks via LSB (`$9000-$9007`) + MSB (`$A000-$A007`) pairs | V / H / Single-lower / Single-upper via `$D001` bits 0–1 | Prescaler-based; `$C003` enable; `$C002` disable/ack; `$C004-$C007` write prescaler/counter/mode; fires on zero-cross with configurable tick source (CPU, A12, PPU-read, CPU-write) | None | None | ✅ | Hardware multiplier at `$5800`/`$5801` (write factors) read result at `$5800` (lo) / `$5801` (hi). Prescaler XOR register (`$C006`) applied when loading `$C004`/`$C005`. Count direction bits 6–7 of `$C001`: 0/3 = no count, 1 = up, 2 = down. Small prescaler mask `$07` when `$C001` bit 2 set, else `$FF`. |
| 91 | Mapper91 | 8 KB granularity (mapper-specific) | 2 KB Switchable | Fixed/header | Mapper IRQ | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper91.rs`; constructor routed via `Mapper91::new`. |
| 92 | Jaleco JF-19/JF-21 | 16 KB switchable at `$8000-$BFFF` + fixed last 16 KB at `$C000-$FFFF`; write `$8000-$FFFF` with bit 7 set updates PRG bank from `D[2:0]` (level-sensitive) | 8 KB switchable at `$0000-$1FFF`; write `$8000-$FFFF` with bit 6 set updates CHR bank from `D[3:0]` (level-sensitive) | Fixed from header | None | None | None | ✅ | Jaleco JF-19/JF-21 register is level-sensitive (not edge-triggered like mapper 72). CHR-RAM fallback is used when CHR-ROM is absent. |
| 93 | Sunsoft-2 IC / 74S161/32 (Fantasy Zone, Shanghai) | 16 KB switchable at $8000–$BFFF + fixed last 16 KB at $C000–$FFFF; bits[6:4] of register | 8 KB CHR-RAM (enable-gated by bit 0; reads return open bus and writes are ignored when disabled) | Fixed from header | None | None | None | ✅ | Register at $8000–$FFFF with bus conflicts. Bits[6:4] select 16 KB PRG bank (up to 8 banks = 128 KB PRG); bit 0 enables CHR-RAM (E=0: reads return 0/open-bus, writes ignored; E=1: normal CHR-RAM access). Power-on state: bank 0, CHR disabled. |
| 95 | Namcot-3425 (Namco 108/118 variant) | MMC3-style 8 KB × 2 switchable + 2 fixed (no IRQ mode switching) | 2 KB × 2 + 1 KB × 4 switchable, mode bits forced to 0 | Derived from R0/R1 bit 5 (1sc lower/upper or horizontal mixed) | None | None | 8 KB optional | ⚠️ | Register interface mirrors writes in `$8000-$FFFF` to `$8000/$8001`; PRG/CHR mode bits are forced off on bank-select writes (`value &= 0x3F`). Hardware edge cases may still differ in untested board variants. |
| 97 | Irem TAM-S1 | 16 KB switchable bank at `$C000-$FFFF`; 16 KB fixed last bank at `$8000-$BFFF` (register writes at `$8000-$BFFF`) | 8 KB CHR-RAM (fixed, unbanked) | Bit 7 of register: 0 = Horizontal, 1 = Vertical | None | None | None | ✅ | Register writes are decoded only in `$8000-$BFFF`; writes in `$C000-$FFFF` are ignored. PRG select uses bits 5:0 of the write value with normal bank wrapping. Power-on maps bank 0 at `$C000` and last bank at `$8000`. |
| 101 | Jaleco JF-10 | Fixed 32 KB (or 16 KB mirrored) at `$8000-$FFFF` | 8 KB switchable via bits [3:0] on writes to `$6000-$7FFF` | Fixed from header | None | None | None | ✅ | No PRG banking; writes only select CHR bank. No IRQ, no expansion audio, no dynamic mirroring. |
| 102 | Deprecated iNES alias (NROM-class) | Fixed 32 KB (or 16 KB mirrored) at `$8000-$FFFF` | Fixed 8 KB CHR (ROM or RAM) | Fixed from header | None | None | Optional (header-defined) | ✅ | Implemented as NROM-equivalent behavior per NesDev mapper 102 guidance (no mapper registers, no banking, no IRQ/audio). |
| 103 | UNL-PEC-586 | Fixed last 32 KB at `$8000-$FFFF`; `$6000-$7FFF` maps selectable 8 KB PRG-ROM bank when PRG-RAM disabled | Fixed 8 KB CHR bank 0 | V/H via `$E000` bit 3 | None | None | 16 KB internal WRAM at `$6000-$7FFF` and `$B800-$D7FF` when enabled | ✅ | `$8000` low nibble selects PRG bank for `$6000-$7FFF`; `$F000` bit 4 toggles PRG-RAM disable (`1`=map PRG-ROM at `$6000-$7FFF`). Writes to WRAM windows remain writable even when PRG-ROM is mapped for reads. |
| 104 | Pegasus 5-in-1 (Golden Five) | 16 KB switchable at `$8000-$BFFF`; paired 16 KB page at `$C000-$FFFF` tracks upper PRG bits | 8 KB CHR-RAM (fixed) | Fixed from header | None | None | None | ✅ | `$C000-$FFFF` writes set PRG low nibble, `$8000-$9FFF` writes with bit 3 set PRG upper bits. CHR is RAM and not bank switched. |
| 105 | NES-EVENT (Nintendo World Championships) | MMC1-style 16 KB/32 KB banking via serial writes (`$8000-$FFFF`) | MMC1-style 4 KB/8 KB CHR banking | H / V / 1scA / 1scB via MMC1 control | CPU-cycle countdown IRQ (armed by CHR bank 0 bit 4 clear) | None | 8 KB | ⚠️ | MMC1-compatible banking/mirroring is implemented and mapper IRQ is exposed. Timer behavior is modeled as a configurable countdown using CHR bank 0 bits [3:0] (simplified vs discrete event-board DIP timing). |
| 106 | Mapper 106 (unlicensed) | 8 KB × 4 switchable via low-nibble register writes (`$8008-$800B`) with mixed masks (`$8008/$800B` force bit 4) | 1 KB × 8 switchable via low-nibble register writes (`$8000-$8007`) with even/odd forcing on registers 0-3 | Fixed from header | 16-bit CPU-cycle up-counter overflow IRQ (set low at `$800E`, high+enable at `$800F`, ack/disable at `$800D`) | None | Optional (header-defined) | ✅ | Implements Mesen2/NesDev register decode (`addr & 0x0F`) including CHR even/odd forcing for registers 0-3. IRQ disables itself after overflow and remains pending until acknowledged. |
| 112 | NTDEC multicart | 8 KB × 2 switchable at `$8000/$A000` + 2 fixed (`-2/-1`) | `R2/R3` provide 2 KB banks at `$0000/$0800`; `R4-R7` provide 1 KB banks at `$1000-$1FFF` with high CHR bit extension from `$C000` (`bits 4-7`) | V / H via `$E000` bit 0 | None | None | None | ✅ | Register select at `$8000` (`value & 7`), data at `$A000`, outer CHR extension at `$C000`; register writes are decoded in `$8000-$FFFF` using `(addr & 0xE001)`. |
| 115 | KS202 / MMC3 variant | MMC3 8 KB banking, with outer PRG override modes via `$4100-$5FFF` even writes | MMC3 1 KB banking with CHR A18 forced by `$4100-$5FFF` odd writes | MMC3 H / V via `$A000` | MMC3 scanline | None | 8 KB | ✅ | `$5000-$5FFF` reads return protection register (`$5080` write). `$6000-$7FFF` remains MMC3 WRAM. Outer PRG register bit 7 enables override; bit 5 selects 32 KB contiguous vs mirrored 16 KB mode. |
| 117 | Fu Guang | 8 KB × 3 switchable at $8000/$A000/$C000 + last 8 KB fixed at $E000 | 8 × 1 KB switchable via $A000-$A007 | V / H via $D000 bit 0 | CPU-cycle down-counter | None | None | ⚠️ | IRQ modeled as CPU-cycle countdown with reload/start/enable/ack controls (`$C001/$C003/$E000/$C002`). |
| 118 | TxSROM (MMC3 variant) | MMC3 8 KB × 2 switchable + 2 fixed to last | MMC3 2 KB × 2 + 1 KB × 4 switchable | Bit 7 of the active CHR bank register selects CIRAM page (NT0/NT1); `$A000` mirroring ignored | MMC3 scanline IRQ | None | 8 KB | ✅ | PRG/CHR banking and IRQ behavior delegate to MMC3. Nametable routing is mapper-provided: per 1 KB nametable page, bit 7 of the active CHR bank register selects NT0/NT1. |
| 129 | Duplicate assignment (Mapper 58 alias) | Same as mapper 58 | Same as mapper 58 | Same as mapper 58 | None | None | None | ✅ | NesDev marks mapper 129 as duplicate assignment of mapper 58; routed to mapper 58 implementation in factory. |
| 132 | TXC 22111 / UNL-22211 | 32 KB switchable (selected by output bit 2) | 8 KB switchable (selected by output bits 1:0) | Fixed from header | None | None | None | ✅ | Register window decoded via `$E103` mask in `$4100-$4FFF`, status readback at `$4100` (`S xor V` + `RRR`), and bank output applied on `$8000` writes per NesDev. |
| 133 | Sachen 72008 / UNL-SA-72008 | 32 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper133.rs`; constructor routed via `Mapper133::new`. |
| 140 | Jaleco JF-11/JF-14 | 32 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper140.rs`; constructor routed via `Mapper140::new`. |
| 155 | MMC1 (SxROM) | 16 KB granularity (mapper-specific) | 4 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mmc1.rs`; constructor routed via `MMC1Mapper::new`. |
| 185 | CNROM with CHR-ROM enable gating (chip select) | 32 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper185.rs`; constructor routed via `Mapper185::new`. |
| 205 | BMC-JC-016-2 (MMC3 multicart) | MMC3 8 KB PRG banks constrained by outer block register | MMC3 1 KB CHR banks constrained by outer block register | MMC3 H / V | MMC3 scanline | None | None | ✅ | Outer block register at `$6000-$7FFF` (`MM`) selects PRG/CHR `AND/OR` masks per NesDev table. No PRG-RAM window at `$6000-$7FFF`. |
| 206 | DxROM / Namco 108/118 / Tengen MIMIC-1 | 8 KB × 2 switchable + 2 banks always fixed to last | 2 KB × 2 + 1 KB × 4 switchable; CHR layout fixed | Fixed H/V or 4-screen (Gauntlet / DRROM) | **None** | None | **None** | ⚠️ | **Namco 108 errata: write to $0000–$1FFF while CPU is executing from $8000–$9FFF can cause a spurious bankswitch.** Submapper 1 (3407/3417/3451 PCBs with unbanked 32 KB PRG) should bypass PRG banking. No IRQ, no WRAM registers at $A000–$FFFF. Ensure those registers are absent (not inherited from MMC3 code). |
| 218 | Magic Floor (homebrew) | Fixed PRG layout (NROM-style unbanked read window) | Fixed 8 KB CHR (ROM/RAM from image) | Fixed from header (H/V/1-screen); FourScreen metadata coerced to 1-screen-lower | None | None | Optional (header-defined) | ⚠️ | Implemented as a fixed mapper with no bank registers, no IRQ, and no expansion audio. Four-screen metadata is reduced to single-screen-lower because mapper context does not retain the iNES bit needed to disambiguate 1-screen A/B fallback. |
| 241 | BxROM variant (150-in-1) | 32 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper241.rs`; constructor routed via `Mapper241::new`. |
| 242 | 43272 (address-latch PRG switch with mirroring control) | 32 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Dynamic / mapper-controlled | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper242.rs`; constructor routed via `Mapper242::new`. |
| 243 | Sachen 74LS374N (SA-020A) | 32 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper243.rs`; constructor routed via `Mapper243::new`. |
| 244 | Decathlon (C-N22M) | 32 KB granularity (mapper-specific) | 8 KB Switchable | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper244.rs`; constructor routed via `Mapper244::new`. |
| 245 | Waixing MMC3 variant | 8 KB granularity (mapper-specific) | 1 KB Fixed/CHR-RAM | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper245.rs`; constructor routed via `Mapper245::new`. |
| 246 | Fong Shen Bang | 8 KB granularity (mapper-specific) | 2 KB Switchable | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper246.rs`; constructor routed via `Mapper246::new`. |
| 251 | Alias of Mapper 45 (Nestopia assignment) | 8 KB granularity (MMC3-derived) | 1 KB Switchable | Dynamic / mapper-controlled | Mapper IRQ | None | up to 8 KB | ⚠️ | Alias route of mapper 45 behavior in `src/cartridge/mapper251.rs` via `Mapper251::new` (delegates to mapper 45 core implementation). |
| 254 | Pikachu Y2K (MMC3 variant with copy protection) | 8 KB granularity (mapper-specific) | 1 KB Switchable | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper254.rs`; constructor routed via `Mapper254::new`. |
| 255 | 110-in-1 Multicart | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper255.rs`; constructor routed via `Mapper255::new`. |
| 257 | PEC-586 FC-Based Computer (DUNDA) | Non-512KB: two 16 KB banks via 128-entry BS_TBL lookup (reg[0]&0x7F index); 512KB: bit4→32KB direct, bit6→8KB at $8000–$9FFF, else custom unscrambled decode | 8 KB CHR fixed bank 0 | Non-512KB: always Vertical; 512KB: (reg[0]&0x18)==0x18 → H, else V | None | None | 8 KB WRAM at $6000–$7FFF | ⚠️ | Registers at $5000–$5FFF decoded by address bits A10–A8. $5000–$5FFF reads return `(open_bus & 0xD8) \| BR_TBL[reg[4]>>4]`. Power-on: 512KB→reg[0]=0x00, else reg[0]=0x0E. Source: FCEUX pec-586.cpp by CaH4e3. **PEC586Hack (PC display mode) not emulated.** |
| 260 | BMC-HPXX (MMC3-variant multicart) | Normal mode: MMC3 8 KiB banking with outer mask (`ex_regs[0]` bit1=0: mask=0x1F/base=`ex_regs[1]&0x10`; bit1=1: mask=0x0F/base=`ex_regs[1]&0x18`); locked mode (`ex_regs[0]` bit2=1): if `(ex_regs[0]&0x0F)==0x04` 16 KiB mirrored `page=(ex_regs[1]&0x1F)<<1`, else 32 KiB `page=(ex_regs[1]&0x1E)<<1` | Normal: MMC3 1 KiB CHR banking with outer mask (`ex_regs[0]` bit0=0: mask=0xFF/base=`ex_regs[2]&0x20`; bit0=1: mask=0x7F/base=`ex_regs[2]&0x30`); locked: 8 KiB fixed per mode bits, base from `ex_regs[2]` and `ex_regs[4]` | Normal: Dynamic via MMC3 `$A000`; locked: `ex_regs[4]` bit2 (1=V, 0=H) | Standard MMC3 scanline counter (A12 rising-edge) | None | 8 KiB at `$6000–$7FFF` | ✅ | Extra registers at `$5000–$5FFF` indexed by `addr & 0x03`; write with bit7 set permanently locks `ex_regs[0..3]`. In locked mode, `$8000–$FFFF` writes update `ex_regs[4]` only. DIP switches (4-bit) not emulated (reads return 0). Source: Mesen2 `BmcHpxx.h`. No known deltas. |
| 262 | Street Heroes (MMC3 variant) | Standard MMC3 PRG banking: two switchable 8 KiB banks at `$8000–$BFFF`, two fixed at `$C000–$FFFF` | CHR-ROM mode (default, `ex_reg` bit6=0): MMC3 1 KiB CHR-ROM banking with per-slot outer bank extension (`ex_reg` bits 3,2,0,1 → slot-pairs 0–1, 2–3, 4–5, 6–7); CHR-RAM mode (`ex_reg` bit6=1): 8 KiB CHR-RAM, PPU address used directly as offset | Dynamic via MMC3 `$A000` | Standard MMC3 scanline counter (A12 rising-edge) | 8 KiB PRG-RAM at `$6000–$7FFF` | None | ✅ | Register at `$4100`: write sets `ex_reg`; read returns `reset_switch` (soft-reset toggle, starts 0x00, toggles to 0xFF on each NES reset). CHR outer bank bits: slot-pair 0–1=`(ex_reg&0x08)<<5`, 2–3=`(ex_reg&0x04)<<6`, 4–5=`(ex_reg&0x01)<<8`, 6–7=`(ex_reg&0x02)<<7`. Source: Mesen2 `MMC3_StreetHeroes.h`. No known deltas. |
| 263 | KoF97 (King of Fighters '97, MMC3 variant) | Standard MMC3 PRG banking: two switchable 8 KiB banks at `$8000–$BFFF`, two fixed at `$C000–$FFFF` | Standard MMC3: eight 1 KiB CHR banks across `$0000–$1FFF` | Dynamic via scrambled write to `$A000` | Standard MMC3 scanline counter (A12 rising-edge) | 8 KiB PRG-RAM at `$6000–$7FFF` | None | ✅ | All writes to `$8000–$FFFF` pass through a bit scrambler before reaching MMC3: `out=(val&0xD8)\|((val&0x20)>>4)\|((val&0x04)<<3)\|((val&0x02)>>1)\|((val&0x01)<<2)`. Address remapping: `$9000`→`$8001`, `$D000`→`$C001`, `$F000`→`$E001`. Writes to `$6000–$7FFF` (PRG-RAM) bypass the scrambler. Source: Mesen2 `MMC3_Kof97.h`. No known deltas. |
| 264 | Cheapocabra (homebrew flash cartridge) | 32 KiB switchable at `$8000–$FFFF`; bank selected by bits 3:0 of register (`$5000–$5FFF` and `$7000–$7FFF` writes); 16 banks max | 8 KiB CHR-RAM switchable; bank selected by bit 4 of register; 2 banks (16 KiB total) | 16 KiB nametable RAM (16 × 1 KiB banks); NT slots 0–3 map to banks base–base+3 where base = 0 (bit5=0) or 8 (bit5=1) | None | None | None | ⚠️ | SST39SF040 flash programming (`$8000–$FFFF` writes) not emulated; writes silently ignored. Open-bus register read side-effect not emulated. Source: Mesen2 `Cheapocabra.h`. |
| 271 | BMC-22026 / TXC MGC-026 4-in-1 multicart | 32 KiB bank at `$8000–$FFFF` selected by bits [5:4] of the data latch written to `$8000–$FFFF` | 8 KiB CHR-ROM bank at PPU `$0000–$1FFF` selected by bits [3:0] of the data latch | Bit 6 of data latch: 0 = Horizontal, 1 = Vertical | None | None | None | ✅ | GNROM-like multicart. Bit 7 is unused. Power-on: bank 0, horizontal mirroring. Source: NesDev wiki (NES 2.0 Mapper 271). No known deltas. |
| 286 | BS-5 (Waixing multicart) | 4 × 8 KiB independently-bankable slots covering `$8000–$FFFF`; slot = `(addr>>10)&3`; bank = `addr&0x0F`; only updated when `addr&0x10` is set (write to `$A000–$AFFF`) | 4 × 2 KiB independently-bankable slots covering `$0000–$1FFF`; slot = `(addr>>10)&3`; bank = `addr&0x1F` (write to `$8000–$8FFF`) | Fixed per iNES header (no runtime control) | None | None | None | ✅ | Write address (not data) used for both PRG and CHR banking. PRG writes are gated by address bit 4 (`addr&0x10`); DIP switches treated as 0. Power-on: all slots map to last bank (−1). Source: Mesen2 `Bs5.h`. No known deltas. |
| 287 | BMC-411120C / K-3088 multicart (MMC3 variant) | Normal mode: MMC3 8 KiB banking masked to 16 banks within outer 128 KiB block (outer bits from exReg[1:0]); NROM mode (exReg bit 3 set): all 4 PRG 8 KiB slots fixed to `(((exReg>>4)&3)\|0x0C)*4 + slot` | MMC3 1 KiB CHR banking with outer bits [8:7] from exReg[1:0] | Dynamic via MMC3 `$A000` | Standard MMC3 scanline counter (A12 rising-edge) | None | None | ✅ | Outer register at `$6000–$7FFF`: latches **address low byte** (not data) as exReg. Bits [1:0] add outer PRG/CHR block; bit 3 = NROM mode; bits [5:4] = NROM inner bank. No PRG-RAM. DIP switch not supported (treated as 0). Source: Mesen2 `MMC3_Bmc411120C.h`. No known deltas. |
| 288 | Gkcx1 (unlicensed) | 32 KiB fixed window at `$8000–$FFFF`; bank selected by bits [4:3] of the write *address* (`(addr>>3)&3`) | 8 KiB CHR bank selected by bits [2:0] of the write *address* (`addr&7`) | Fixed per iNES header (no runtime control) | None | None | None | ✅ | Write to any `$8000–$FFFF` address uses the address itself (not the data byte) to select banks. Implemented in `src/cartridge/mapper288.rs`. Source: Mesen2 `Gkcx1.h`. No known deltas. |
| 291 | Kasheng 2-in-1 Multicart (Mortal Kombat 6 + Samurai Spirits) | MMC3 inner banking within 128 KiB outer block (M=0); or 32 KiB direct via PP bits (M=1); outer bank bit (O, bit 6 of `$6000` register) selects which 128 KiB half | MMC3 inner banking within 256 KiB outer CHR block; outer CHR bit (O) selects which 256 KiB half | Dynamic via MMC3 `$A000` | Standard MMC3 scanline counter (A12 rising-edge) | None | None | ✅ | Outer register at `$6000–$7FFF`: bit 6=O (outer PRG+CHR), bit 5=M (0=MMC3 inner, 1=32 KiB direct), bits 2:1=PP (32 KiB inner bank). Always responds even when MMC3 WRAM protect is off. Implemented in `src/cartridge/mapper291.rs`. Source: NesDev wiki NES 2.0 Mapper 291. No known deltas. |
| 292 | BMW8544 / Dragon Fighter (Thin Chen / JY protected MMC3 variant) | MMC3 R6/R7 8 KiB banking; `$8000–$9FFF` overridden by bits 4:0 of last write to `$6000–$7FFF` | Fixed layout regardless of MMC3 CHR mode: `$0000–$07FF` = 2 KiB `((R0&0xFE)>>1) XOR sprite_xor`; `$0800–$0FFF` = 2 KiB `((R1&0xFE)>>1) XOR sprite_or`; `$1000–$1FFF` = 4 KiB `bg_chr&0x3F` | Dynamic via MMC3 `$A000` | Standard MMC3 scanline (A12 rising-edge) | None | None | ✅ | Protection at `$6000–$7FFF`: writes set prot_reg (bit5=mode, bits4:0=extra PRG bank) and cpu_latch; reads trigger CHR update: BG mode → `bg_chr=latch&0x3F`, `sprite_or=(latch&0x40)<<1`; sprite mode → `sprite_xor=latch`. Source: FCEUX libretro-fceumm / Furbtendulator. No known deltas. |
| 294 | 63-1601 (強捧 16-in-1 multicart) | UNROM-style 16 KiB inner bank at `$8000–$BFFF` via bits 2:0 of value written to `$8000–$FFFF`; fixed last 16 KiB in outer block at `$C000–$FFFF`; outer PRG block (A20..A17) selected by bits 3:0 of outer register | 8 KiB CHR-RAM (fixed bank 0, unbanked) | Outer register bit 4: 0 = Vertical, 1 = Horizontal | None | None | None | ✅ | Outer register written to any address where `addr & 0xC100 == 0x4100`. Inner writes use bus conflicts. Source: NesDev wiki NES 2.0 Mapper 294. No known deltas. |
| 300 | BMC-190in1 (190-in-1 multicart) | 16 KiB bank selected by bits [4:2] of write data, mirrored at both `$8000` and `$C000` | 8 KiB CHR bank selected by same bits [4:2] | Bit 0 of write data: 0 = Vertical, 1 = Horizontal | None | None | None | ✅ | Implemented in `src/cartridge/mapper300.rs`; constructor routed via `Mapper300::new`. Source: Mesen2 `Bmc190in1.h`. No known deltas. |
| 302 | Mapper302 | 2 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Dynamic / mapper-controlled | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper302.rs`; constructor routed via `Mapper302::new`. |
| 307 | Kaiser KS7037 | 4 KB granularity: $8000–$9FFF switchable (reg6×2, reg6×2+1); $A000–$AFFF fixed (−4); $C000–$DFFF switchable (reg7×2, reg7×2+1); $E000–$FFFF fixed (last 2 pages); $7000–$7FFF fixed page 15 | 8 KiB CHR-RAM (unbanked) | Per-nametable: regs[2–5] bit 0 selects VRAM page for each of the 4 NTs | None | None | 8 KiB WRAM (page 0 at $6000–$6FFF; page 1 at $B000–$BFFF) | ✅ | Registers at $8000 (index select) / $8001 (data); $A000–$BFFF not writable to registers. No known deltas. |
| 308 | UNL-TH2131-1 (Batman bootleg) | Two 8 KiB switchable banks ($8000 → $8000–$9FFF, $A000 → $A000–$BFFF); second-to-last 8 KiB fixed at $C000–$DFFF; last 8 KiB fixed at $E000–$FFFF | Eight 1 KiB switchable CHR banks via low/high nibble registers at $B000–$E003 | H/V programmable: $9000 bit 0 (0 = Vertical, 1 = Horizontal) | Custom M2-cycle: 12-bit low counter + 4-bit high counter; $F000 ack/disable/reset, $F001 enable, $F003 bits[7:4] load high counter; IRQ asserted when high=0 and low-counter bit11=0 | None | 8 KiB at $6000–$7FFF (always-on, VRC2 style) | ✅ | VRC2b clone (Mapper 23 Submapper 3) with custom IRQ added. Source: NesDev wiki (NES 2.0 Mapper 308) + libretro-fceumm_next `mapper308.c` reference. $F000–$FFFF writes handled by IRQ logic; all other PRG writes delegate to VRC2b inner mapper. |
| 313 | ResetTxrom (MMC3-based 4-in-1 multicart) | 8 KiB × 2 switchable + 2 fixed (MMC3), extended by outer counter: `physical = (reset_counter << 4) \| (mmc3_bank & 0x0F)` | 2 × 2 KiB + 4 × 1 KiB switchable (MMC3), extended by: `physical = (reset_counter << 7) \| (mmc3_bank & 0x7F)` | H / V via MMC3 `$A000` write | Scanline (A12) via MMC3 | None | 8 KiB optional | ✅ | MMC3-based 4-in-1 multicart; 2-bit reset counter selects game slot (0–3). Soft reset increments counter; hard reset (power-on) clears to 0. Hard/soft distinction detected via `initialize_ram()` call. Source: Mesen2 `ResetTxrom.h`. No known deltas. |
| 314 | BMC-64IN1NOREPEAT (64-in-1 no-repeat multicart) | Three-mode 16 KiB banking via 4 registers at $5000–$5003 (also $8000–$FFFF writes reg[3]): reg[0] bit 7=1 (outer mode): reg[1] bit 7=1 → 32 KiB pair at `$8000/$C000` = `(reg[1]&0x1F)<<1` and `+1`; reg[1] bit 7=0 → 16 KiB NROM both windows = `((reg[1]&0x1F)<<1)\|((reg[1]>>6)&1)`; reg[0] bit 7=0 (inner mode): only `$C000` updated | 8 KiB CHR bank: `(reg[2]<<2)\|((reg[0]>>1)&0x03)` | reg[0] bit 5: 1 = Horizontal, 0 = Vertical | None | None | None | ✅ | Reset state: reg[0]=0x80, reg[1]=0x43, reg[2]=0, reg[3]=0 → page0=page1=bank 7, CHR bank 0, Vertical mirroring. Source: Mesen2 `Bmc64in1NoRepeat.h`. No known deltas. |
| 319 | HP898F (4-in-1 multicart) | Two 16 KiB PRG pages: `$8000–$BFFF` = `prg_reg & ~prg_mask`; `$C000–$FFFF` = `prg_reg \| prg_mask`; `prg_reg = (reg1 >> 3) & 7`; `prg_mask = (reg1 >> 4) & 4` (bit 6 enables 32 KiB pair) | 8 KiB switchable CHR: `chr_bank = (reg0 >> 4) & 7`; `chr_mask = ((reg0 & 1) << 2) \| (reg0 & 2)`; `chr_page = chr_bank & ~chr_mask` | Dynamic: reg1 bit 7 = 1 → Vertical, 0 → Horizontal | None | None | None | ✅ | Registers written when `(addr & 0x6000) == 0x6000` (active at `$6000–$7FFF` and `$E000–$FFFF`); reg index = `(addr & 0x04) >> 2`. Source: Mesen2 `Hp898f.h`. No known deltas. |
| 324 | Farid UNROM-8 (homebrew) | 16 KiB switchable at `$8000–$BFFF` via reg bits 2:0 (inner) + 6:4 (outer block, latched on bit-7 rising edge); fixed last 16 KiB in outer block at `$C000–$FFFF` | 8 KiB CHR-RAM (fixed bank 0, unbanked) | Fixed from header | None | None | None | ✅ | Register written to `$8000–$FFFF` with bus conflicts. Outer-bank bits 6:4 latched when bit-7 transitions 0→1 and lock bit (bit 3) is clear. Lock bit (bit 3) prevents further outer-bank changes once set. Soft-reset outer-bank retention is not distinguished from hard reset in this implementation. |
| 325 | Mali Splash Bomb (MMC3 variant) | MMC3 PRG banking with bits 2 and 3 of each bank register swapped before use | MMC3 CHR banking with bits 1 and 5 of each bank register swapped before use | Controlled by MMC3 (same as standard MMC3 mirroring) | MMC3-compatible IRQ | None | None | ✅ | MMC3 variant with three hardware scramblings: (1) write-address bits remapped so bit0 = bit2\|bit3 (≥$C000) or bit0 = bit3 (<$C000); (2) PRG bank bits 2↔3 swapped; (3) CHR bank bits 1↔5 swapped. Source: Mesen2 `MMC3_MaliSB.h`. |
| 326 | Mapper326 | 8 KB granularity (mapper-specific) | 1 KB Fixed/CHR-RAM | Fixed/header | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper326.rs`; constructor routed via `Mapper326::new`. |
| 327 | 10-24-C-A1 (unknown board) | Fixed 32 KiB at $8000–$FFFF | Fixed 8 KiB at $0000–$1FFF | Fixed from header | None | None | None | ⚠️ | Specification unavailable: NesDev wiki blocked, Mesen2 unimplemented (`break;`), FCEUX has no implementation. Implemented as a minimal fixed-PRG/CHR stub so ROMs can instantiate without panicking. |
| 328 | RT-01 (Thunderbirds) | Fixed 32 KiB at $8000–$FFFF (two 16 KiB pages, both bank 0) | Fixed 8 KiB (four 2 KiB pages, all bank 0) | Fixed from header | None | None | None | ⚠️ | Copy-protection reads at $CE80–$CEFF and $FE80–$FEFF return `0xFF` (a constant satisfying the hardware's `0xF2 \| (rand & 0x0D)` pattern). Games relying on value variation across reads may not pass the protection check. |
| 329 | EDU2000 | 32 KiB switchable at $8000–$FFFF; bank selected by bits 4:0 of register | Fixed 8 KiB (bank 0 always) | Fixed from header | None | None | 32 KiB (4 pages of 8 KiB; page selected by bits 7:6) | ✅ | Single 8-bit register written to $8000–$FFFF. Bits 4:0 = PRG bank; bits 7:6 = PRG-RAM page. Source: Mesen2 `Edu2000.h`. |
| 330 | Mapper330 | 32 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | None | None | up to 8 KB | ⚠️ | Implemented in `src/cartridge/mapper330.rs`; constructor routed via `Mapper330::new`. |
| 332 | Super 40-in-1 (BMC-WS) | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper332.rs`; constructor routed via `Mapper332::new`. |
| 335 | BMC-CTC-09 (10-in-1 multicart) | 16 KB granularity (mapper-specific) | 8 KB Switchable | Dynamic / mapper-controlled | None | None | None | ⚠️ | Implemented in `src/cartridge/mapper335.rs`; constructor routed via `Mapper335::new`. |
| 337 | BMC-CTC-12IN1 (12-in-1 multicart, 1991 New Star Co. Ltd.) | Two 16 KB windows: `$8000` = outer\|(prgchr[0]&7) or outer\|(prgchr[0]&6) in NROM mode; `$C000` = outer\|7 or outer\|(prgchr[0]&6)\|1 in NROM mode; outer = (ctrl[1:0])<<3 | Two 4 KB windows: `$0000` = outer_chr\|(prgchr[0]>>3); `$1000` = outer_chr\|(prgchr[1]>>3); outer_chr = (ctrl[1:0])<<5 | `ctrl` bit 2: 0 = Vertical, 1 = Horizontal | None | None | None | ✅ | Registers prgchr[0] at `$A000-$BFFF`, prgchr[1] at `$C000-$DFFF`, ctrl (low nibble) at `$E000-$FFFF`; writes to `$8000-$9FFF` are ignored. NROM mode (ctrl bit 3 = 1) makes both PRG windows a consecutive pair. |
| 338 | BMC-SA005-A (16-in-1 / 200-300-600-1000-in-1 multicart) | 16 KB bank selected by bits [2:0] of write data, mirrored at both `$8000` and `$C000` | 8 KB CHR bank selected by same bits [2:0] | Bit 3 of write data: 0 = Horizontal, 1 = Vertical | None | None | None | ✅ | Implemented in `src/cartridge/mapper338.rs`; constructor routed via `Mapper338::new`. |
| 339 | BMC-K-3006 (MMC3+NROM multicart) | Outer register (`$6000-$7FFF` address latch) selects `CC` outer PRG block and NROM mode bits; NROM-128 uses `bba` 16 KiB bank mirrored at `$8000/$C000`, NROM-256 uses `bb` + CPU A14; MMC3 mode (`M=1`) uses MMC3 inner 8 KiB pages combined with outer `CC` | MMC3 1 KiB CHR pages with 7-bit inner bank masked and extended by outer `CC` | MMC3-controlled H / V | MMC3 scanline | None | MMC3 PRG-RAM window | ⚠️ | NES 2.0 submapper-specific NROM-256 selection is implemented (submappers 0-4). Outer register writes are accepted only when MMC3 PRG-RAM is writable and mode bit selects NROM path. `P` solder-pad PRG A1..A0 menu-selection behavior is not emulated. |
| 340 | BMC-K-3036 (35-in-1 multicart) | Mode-latched by write address: UNROM (`A5=0`) maps `$8000` to `A[4:0] OR D[2:0]` and fixes `$C000` to `A[4:0] OR 7`; NROM-128 (`A5=1`) mirrors `A[4:0]` at both `$8000/$C000` | 8 KB CHR-RAM (unbanked) | Horizontal only when `(A & 0x25) == 0x25`, otherwise Vertical | None | None | None | ✅ | Register is address/data latched on writes to `$8000-$FFFF`: address low bits provide outer bank + mode + mirroring selector; data bits `D[2:0]` provide inner bank in UNROM mode. CHR treated as fixed 8 KB RAM and any dumped CHR payload is used as initial RAM seed for compatibility. |
| 341 | BMC-TJ-03 (4-in-1 multicart) | Single 16 KB bank selected by address latch and mirrored at `$8000` and `$C000` | 8 KB CHR bank selected by same address latch bits | Address-latched bit (`A1`): 0 = V, 1 = H | None | None | None | ✅ | Write address in `$8000-$FFFF` latches register: bits `A10..A8` select both PRG and CHR bank (`BBB`), bit `A1` selects mirroring. Write data byte is ignored. |
| 342 | COOLGIRL multicart (minimal baseline) | 32 KB granularity (mapper-specific) | 8 KB Fixed/CHR-RAM | Fixed/header | None | None | up to 32 KB | ⚠️ | Implemented in `src/cartridge/mapper342.rs`; constructor routed via `Mapper342::new`. |
| 343 | Reset-based 4-in-1 multicart (NES 2.0 reassignment) | NROM-128 style per game: selected 16 KB bank mirrored at `$8000` and `$C000` | Selected 8 KB CHR bank per game | Fixed from header | None | None | None | ✅ | Routed as a mapper 60-compatible alias. Internal 2-bit game index advances on reset and wraps after 4 entries. |
| 344 | BMC-GN-26 (MMC3 multicart) | MMC3 mode (`S=0`): 8 KB MMC3 banks with outer `BA` selecting PRG A18..A17; NROM mode (`S=1`): entire `$8000-$FFFF` derived from reg6 bits [3:1] + CPU A13 (mirrored 16 KB pair) with same outer `BA` extension | 1 KB MMC3 CHR banks with outer A18 from `B`; A17 source selected by `M` (`0`=MMC3 A17, `1`=outer `A`) | MMC3 H / V via `$A000` | MMC3 scanline | None | 8 KB | ⚠️ | Outer register is address-latched from `$6000-$7FFF` low nibble (`SMBA`) and is writable only when PRG-RAM is writable (`$A001.7` gate via MMC3 RAM control). Solder-pad test side effect on `S=1` (possible PRG-ROM disable) is not emulated. Common dump bank-order quirk (`0,3,1,2`) is not auto-corrected. |
| 345 | BMC-L6IN1 (New Star 6-in-1) | Hybrid mode: MMC3-style 8 KB banking (`reg[3:2]!=0`) or AxROM-style 32 KB banking (`reg[3:2]==0`) with outer 128 KB select (`reg[7:6]`) | 8 KB CHR-RAM banked as MMC3 1 KB pages | MMC3 H/V, or one-screen override via `reg[5:4]` | MMC3 scanline | None | 8 KB (MMC3 PRG-RAM window) | ✅ | Mode register at `$6000-$7FFF` is accepted only when PRG-RAM is writable (MMC3-style gate). MMC3 registers at `$8000-$FFFF` are always decoded. One-screen mirroring forces lower/upper nametable when `reg[5]=1`; otherwise mirroring follows MMC3 `$A000` control. |
| 346 | Kaiser KS7012 | Single 32 KB PRG page at `$8000-$FFFF`; power-on page 1; exact address writes select bank (`$E0A0`→0, `$EE36`→1) | Fixed 8 KB CHR page 0 | Fixed from header | None | None | None | ✅ | Implemented from Mesen/NesDev behavior as a 2-state PRG selector. Write value is ignored; only exact write addresses are decoded. |
| 347 | Kaiser UNL-KS7030 (FDS conversion) | Split map: `$7000-$7FFF` 4 KB bank via `$8000[2:0]`; `$6C00-$6FFF`+`$C000-$CBFF` from 4 KB bank via `$9000[3:0]`; `$8000-$B7FF` and `$D800-$FFFF` fixed | Fixed 8 KB CHR page 0 | H / V via `$8000[3]` | None | None | 8 KB split windows (3K + 2K + 3K) | ⚠️ | Implements NesDev July 2020 mask-ROM layout (first 64 KB = `$9000` banks, next 32 KB = `$8000` banks, final 32 KB fixed overlay). Writes to FDS sound registers are currently not synthesized into expansion audio output. |
| 348 | BMC-830118-C (MMC3 variant) | MMC3 banking with outer extension bits from `$6800-$68FF` (`reg[3:2]`) | MMC3 1 KB banking with high bits from `reg[3:2]` | MMC3 H / V via `$A000` | MMC3 scanline IRQ | None | None | ✅ | Outer register extends PRG (`(reg&0x0C)<<2 | (page&0x0F)`) and CHR (`(reg&0x0C)<<5 | (page&0x7F)`). Special mode when `reg[3:2]=0b11`: upper PRG windows are forced to `0x32 | low_nibble` derived from the two lower windows. |
| 349 | BMC G-146 | Address-latched 16 KB pages (mode-dependent 32 KB pair / mirrored / split-fixed) | Fixed 8 KB bank 0 | H / V via A7 | None | None | None | ✅ | Register is write address on `$8000-$FFFF`. `A11=0,A6=0` selects 32 KB pair via `addr&0x1E`; `A11=0,A6=1` mirrors 16 KB `addr&0x1F`; `A11=1` maps low=`addr&0x1F`, high=`(addr&0x18)|0x07`. |
| 350 | BMC-891227 multicart | Fixed 8 KB bank #1 at $6000 + mode-dependent 16 KB windows at $8000/$C000 (NROM-128, NROM-256, UNROM) | 8 KB CHR-RAM (unbanked) | H / V via outer register bit 6 | None | None | None | ⚠️ | Outer register at `$8000-$BFFF`, inner register at `$C000-$FFFF`. Chip-select bit (outer bit 3) is applied in UNROM modes (2/3). CHR-RAM writes are blocked in NROM modes and enabled in UNROM modes. Additional undocumented board quirks are not currently emulated. |
---



## Unimplemented Mappers of Note

The following mappers are **not** present in neser's `mapper_registry!` and represent the most
commonly-played or interesting gaps. The full NesDev mapper list is at
<https://www.nesdev.org/wiki/Mapper>.

| # | Hardware Name | Notable Games | Description / Why it Matters |
|---|---------------|---------------|-------------------------------|
| 94 | Senjou no Ookami / Capcom | Senjou no Ookami | UNROM clone with register at $8000, bus conflicts |
| 105 | NES-EVENT (MMC1 variant) | Nintendo World Championships 1990 | MMC1 variant with additional timer/counter |
| 112 | NTDEC multicart | Asder 20-in-1 | 4-bit PRG and CHR multi-register set |
| 113 | Multicart (NINA-03 style) | HES 6-in-1, Sachen Glorys | PRG+CHR register at $4100/$6000 |
| 119 | TQROM | Pin Bot, High Speed | MMC3 + CHR mixed ROM+RAM banks |
| 152 | Bandai (FCG-like, 1-screen) | Gegege no Kitarou 2 | Mapper 70 variant with 1-screen mirroring |
| 153 | Bandai (LZ93D50 + WRAM) | Famicom Jump II | Mapper 16 variant with 8 KB WRAM instead of EEPROM |
| 154 | Namco 118 + 1-screen | Devil Man | Mapper 88 + CIRAM A10 control |
| 157 | Bandai Datach | Dragon Ball Z series (Datach) | Mapper 16 + barcode reader interface |
| 159 | Bandai (LZ93D50 + 24C01) | SD Gundam Gaiden | Mapper 16 variant with 128-byte EEPROM |
| 163 | Nanjing (FC protection) | Diablo knockoff, etc. | Complex protection registers |
| 180 | Nihon Bussan (UNROM variant) | Crazy Climber | UNROM-like with fixed low bank, switchable high bank |
| 184 | Sunsoft-1 | Atlantis no Nazo (some), Wing of Madoola | Fixed 16 KB PRG, 2 × 4 KB CHR switchable |
| 189 | TXC (PCB 01-22000-400) | Armadillo | MMC3-like with external PRG register |
| 232 | Camerica BF9096 (Quattro games) | Quattro Adventure, etc. | Mapper 71 with additional outer bank register |

---

## Key Compatibility Notes

### Expansion Audio Summary

| Mapper | Chip | Status |
|--------|------|--------|
| 5 | MMC5 8-ch PCM | Implemented |
| 19 | Namco 163 wavetable | Implemented |
| 24 / 26 | VRC6 pulse + sawtooth | Implemented |
| 69 | Sunsoft 5B (YM2149) | **Not implemented** → Gimmick! music broken |
| 85 | VRC7 FM (YM2413) | Implemented (mapper supported; FM audio stubbed with silence) |

### Save Game (EEPROM/SRAM) Status

| Mapper | Save Type | Status |
|--------|-----------|--------|
| 1 | PRG-RAM battery-backed | Implemented |
| 4 | PRG-RAM battery-backed | Implemented |
| 16 (sub 5) | I²C 24C02 EEPROM | **Not implemented** – DBZ II/III saves broken |
| 69 | Switchable PRG-RAM | Implemented (ROM/RAM select bit) |

---

*Last updated: cross-referenced against NesDev wiki July 2025.*
